import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#32d095",
      contrastText: "#fff",
    },
    secondary: {
      main: "#525371",
      contrastText: "#525371cb",
    },
  },
});
